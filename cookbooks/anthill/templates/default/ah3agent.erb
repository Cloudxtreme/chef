#!/bin/bash
if [ "$USER" != "builder" ]; then
  su -l builder -c "/usr/local/anthill3/<%= node[:anthill][:hostname] %>/bin/ah3agent.orig $@"
else 
  /usr/local/anthill3/<%= node[:anthill][:hostname] %>/bin/ah3agent.orig $@
fi
